CREATE TABLE  `gastroscore` (
 `gastroscoreid` INT UNSIGNED NOT NULL AUTO_INCREMENT PRIMARY KEY ,
 `gastronote_id` INT( 10 ) NOT NULL ,
 `upvote_count` INT( 6 ) NOT NULL ,
 `save_count` INT( 6 ) NOT NULL ,
 `recommend_count` INT( 6 ) NOT NULL ,
 `total_count` INT( 6 ) NOT NULL
);


CREATE TABLE  `gastroscore_points` (
 `gastroscorepointid` INT UNSIGNED NOT NULL AUTO_INCREMENT PRIMARY KEY ,
 `upvote_point` INT( 10 ) NOT NULL ,
 `save_point` INT( 6 ) NOT NULL ,
 `recommend_point` INT( 6 ) NOT NULL ,
 `total point` INT( 6 ) NOT NULL
);


ALTER TABLE  `gastroscore_points` ADD  `gastronote_id` INT( 10 ) NOT NULL AFTER  `gastroscorepointid`;

ALTER TABLE  `gastroscore_points` CHANGE  `total point`  `total_point` INT( 6 ) NOT NULL;

ALTER TABLE  `gastroscore_points` DROP  `total_point`;